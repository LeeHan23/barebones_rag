# Server
fastapi
uvicorn[standard]
python-dotenv
python-multipart
requests

# LangChain (pinned to match bare_NutriChatbot)
langchain==0.1.20
langchain-community==0.0.38
langchain-core==0.1.52
langchain-openai==0.1.3

# LLM
openai>=1.0.0
tiktoken

# Embeddings
sentence-transformers

# Vector store (file-based, no server needed)
chromadb

# Document parsing
pypdf
python-docx

# LoRA adapter support (optional — only needed if EMBEDDING_ADAPTER_PATH is set)
# peft
# transformers
# torch
